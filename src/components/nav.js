import React from "react";
import logo from "../img/Vector.png"

export default function Nav(){
    return(
        <nav>
            <img src={logo} alt="" />
            
        </nav>
    )
}